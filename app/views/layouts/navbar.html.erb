
<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container-fluid">
	  <div class="row">
	    <div class="col-xs-2 visible-xs-block border">
	        <a class="navbar-brand" href="#page-top">Owls R'us</a>	
	    </div>
	    <div class="col-xs-8 visible-xs-block border">
	      	<!-- hamburger menu -->
	        <div class="navbar-header page-scroll">
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
              </button>
            </div> <!--closes navbar-header page scroll-->                        
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse pull-right" id="bs-example-navbar-collapse-1">          
              <div>
                <ul class="nav navbar-nav navbar-right">
                    <li class="hidden">
                        <a href="#page-top"></a>
                    </li>
                    <li class="page-scroll">
                        <%= link_to "Products", products_path %>
                    </li>
                    <li class="page-scroll">
                        <%= link_to "about", static_pages_about_path %> 
                    </li>
                    <li class="page-scroll">
                        <%=link_to "Contact", static_pages_contact_path %> 
                    </li>
                    <li class="login">
                      <% if user_signed_in? %>
                        <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
                      <% else %>
                        <%= link_to('Login', new_user_session_path) %>
                      <% end %>
                    </li>                    
                </ul>
              </div> 
            </div> <!-- /.navbar-collapse -->  
	    </div> <!-- closes: "col-xs-8 visible-xs-block border"--> 
	    <div class="col-xs-2 visible-xs-block border">search
	      <div class="search pull-left">
            <%= form_tag("/products", method: "get") do %>
            <form class="navbar-form navbar-right" role="search">
                <%= label_tag(:q, "Search for") %>
                <%= text_field_tag(:q) %>
                <%= submit_tag "Search", class: 'btn btn-default navbar-btn' %> 
            </form>
              <% end %>
          </div>    
	    </div> <!--col-xs-2 visible-xs-block border-->
		<div class="col-sm-3 col-lg-3 visible-sm-block visible-md-block border">
	      <a class="navbar-brand" href="#page-top">Owls R'us</a>
	    </div>
	    <div class="col-sm-6 col-lg-6 visible-sm-block visible-md-block border">
	      	<!-- hamburger menu -->
	        <div class="navbar-header page-scroll">
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
              </button>
            </div> <!--closes navbar-header page scroll-->                        
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse pull-right" id="bs-example-navbar-collapse-1">          
              <div>
                <ul class="nav navbar-nav navbar-right">
                    <li class="hidden">
                        <a href="#page-top"></a>
                    </li>
                    <li class="page-scroll">
                        <%= link_to "Products", products_path %>
                    </li>
                    <li class="page-scroll">
                        <%= link_to "about", static_pages_about_path %> 
                    </li>
                    <li class="page-scroll">
                        <%=link_to "Contact", static_pages_contact_path %> 
                    </li>
                    <li class="login">
                      <% if user_signed_in? %>
                        <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
                      <% else %>
                        <%= link_to('Login', new_user_session_path) %>
                      <% end %>
                    </li>                    
                </ul>
              </div> 
            </div> <!-- /.navbar-collapse -->  
	    </div> <!-- closes: "col-xs-8 visible-xs-block border"-->
		<div class="col-sm-3 col-lg-6 visible-sm-block visible-md-block border">
	      <div class="search pull-left">
            <%= form_tag("/products", method: "get") do %>
            <form class="navbar-form navbar-right" role="search">
                <%= label_tag(:q, "Search for") %>
                <%= text_field_tag(:q) %>
                <%= submit_tag "Search", class: 'btn btn-default navbar-btn' %> 
            </form>
            <% end %>
          </div>  	
		</div> <!--closes: col-sm-3 visible-sm-block visible-md-block border-->
	  </div>
	</div> 
</nav>



<!-- old navigation --> 
  <!-- Navigation -->
  <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header page-scroll">
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
              </button>
              <a class="navbar-brand" href="#page-top">Owls R'us</a>
          </div>

          <div class="search pull-left">
            <%= form_tag("/products", method: "get") do %>
            <form class="navbar-form navbar-right" role="search">
                <%= label_tag(:q, "Search for") %>
                <%= text_field_tag(:q) %>
                <%= submit_tag "Search", class: 'btn btn-default navbar-btn' %> 
            </form>
              <% end %>
          </div>           

          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse pull-right" id="bs-example-navbar-collapse-1">          
              <div>
                <ul class="nav navbar-nav navbar-right">
                    <li class="hidden">
                        <a href="#page-top"></a>
                    </li>
                    <li class="page-scroll">
                        <%= link_to "Products", products_path %>
                    </li>
                    <li class="page-scroll">
                        <%= link_to "about", static_pages_about_path %> 
                    </li>
                    <li class="page-scroll">
                        <%=link_to "Contact", static_pages_contact_path %> 
                    </li>
                    <li class="login">
                      <% if user_signed_in? %>
                        <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
                      <% else %>
                        <%= link_to('Login', new_user_session_path) %>
                      <% end %>
                    </li>                    
                </ul>
              </div> 
          </div> <!-- /.navbar-collapse -->
      </div> <!-- /.container-fluid -->   
  </nav>



