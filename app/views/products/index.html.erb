<p id="notice"><%= notice %></p>

<h1 class="titelproductpage">Waiting for new owners</h1>

<p><% @test_variable %></p>

<!--
<table class="producttable">
  <thead>
    <tr class="table">
      <th>Name</th>
      <th>Description</th>
      <th>Image url</th>
      <th>Colour</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr class="table">
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.image_url %></td>
        <td><%= product.colour %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<div class="container-fluid">
  <div class="row">
    <% @products.each do |product| %>
      <div class="col-sm-2 col-md-4">
        <h2><%= product.name %></h2>
        <div class="owlinfotable text-center">
            <p><%= image_tag(product.image_url, class:"img-responsive owlimage") %></p>
            <p><i><%= product.latin_name %></i></p>
            <p><%= product.description %></p>
            <p><%= product.colour %></p>
            <p> € <%= product.price %></p>
        </div>
      </div> <!--column-->
    <% end %>
  </div>
</div>

<br>

<%= link_to 'New Product', new_product_path %>
